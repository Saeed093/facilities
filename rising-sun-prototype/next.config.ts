import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /** Hide the Next.js logo badge in the corner during local development */
  devIndicators: false,
};

export default nextConfig;
